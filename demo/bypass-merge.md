# Bypass merge demo

This pull request exists to test the admin bypass merge in Linear.

The `main` branch requires one approving review, so the pull request stays blocked until an admin bypasses the rule.
