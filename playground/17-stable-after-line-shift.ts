/** Search behavior. */
export const search = {
  enabled: true,
  label: "Search",
  minimumLength: 3,
  persist: true,
};

/** Preview behavior. */
export const preview = {
  enabled: true,
  label: "Preview",
  contextLines: 3,
  persist: true,
};

/** Navigation behavior. */
export const navigation = {
  enabled: true,
  label: "Navigation",
  pageSize: 20,
  persist: true,
};

/** History behavior. */
export const history = {
  enabled: true,
  label: "History",
  limit: 25,
  persist: true,
};
