/** Search behavior. */
export const search = {
  enabled: true,
  label: "Search",
  minimumLength: 2,
  persist: true,
};

/** Preview behavior. */
export const preview = {
  enabled: true,
  label: "Preview",
  contextLines: 6,
  persist: true,
};

/** Navigation behavior. */
export const navigation = {
  enabled: true,
  label: "Navigation",
  pageSize: 50,
  persist: true,
};

/** History behavior. */
export const history = {
  enabled: true,
  label: "History",
  limit: 100,
  persist: true,
};
